# 编译器链已冻结

**授权人**: [你的名字]
**日期**: [YYYY-MM-DD]
**GPG 密钥**: [待你生成 GPG 后填入密钥 ID]

---

## 冻结声明

我，[你的名字]，**特此** 在此刻冻结 YOYO 编译器链。下面列出的文件现在是**只读**的，不会再被修改。未来的开发通过 yoyo（Rust 验证对等端）和其他仓库进行。

## 冻结的文件

```
yoyo-js/
├── src/
│   ├── yoyo.js           （M0，162 行，已审计）
│   ├── （yoyo-gen.js：pre-v3 生成器，lockdown 后删除）
│   ├── encode-x64.js     （x64 编码参考）
│   ├── pe-builder.js     （PE 模板）
│   ├── elf-builder.js    （ELF 模板）
│   └── [所有其他 JS 文件]
└── projects/
    └── yoyo-blob.ty      （M3 输出源，~1000 行压缩）
```

## 冻结状态

| 属性 | 值 |
|------|-----|
| yoyo.js SHA-256 | [哈希] |
| yoyo-blob.ty 行数 | [行数] |
| M0≡M1≡M2≡M3 已验证 | 是（见 04-SELF_HOST_VERIFIED.md）|
| DDC 已验证 | 是（见 05-DDC_VERIFIED.md）|
| ISA 已审计 | 是（见 02-audit-isa.md）|
| 种子已审计 | 是（见 01-audit-yoyo-js.md）|

## "冻结"意味着什么

在此之后：

1. **不修改代码** —— yoyo.js、yoyo-blob.ty（yoyo-gen.js lockdown 后已删除）
2. **没有"改进"** 那些不是安全关键的
3. **未来的工作** 通过 yoyo（Rust）或新仓库进行
4. **如果任何冻结的文件因任何原因改变了**，需要重新审计

## 为什么冻结？

编译器是**自举且已验证的**。任何进一步的更改：
- 冒着破坏 DDC 验证的风险
- 冒着引入漂移的风险
- 冒着给攻击者新攻击面的风险

**冻结是最安全的状态。** 改进通过 Rust yoyo 进行，独立验证。

## 如果我需要修改冻结的文件？

不能。任何对冻结文件的更改都需要：

1. 对更改文件的新审计
2. 新的 DDC 验证
3. 新的签收（此文件更新）
4. 新的 commit，用 GPG 签名
5. 公开宣布

**不要更改冻结的文件。改为添加新仓库。**

## 我的承诺

作为此冻结的授权人，我承诺：

1. **维护** `docs/custody/` 中的信任记录
2. **在合理时间内响应** 安全问题
3. **按照每个审计文件中的时间表重新审计**
4. **不静默地更改** 冻结状态
5. **发布新版本** 使用适当的版本控制和签名
6. **记录决策** 在 `docs/decisions/`

## 决定

**我在此刻冻结 YOYO 编译器链。**

这是 YOYO 正式"通过我的签名而可信"的时刻。在此之前，架构使 YOYO 可审计。**在此之后，我的签名使 YOYO 可信。**

## 签名

签名: [你的名字]
日期: [YYYY-MM-DD]
GPG 密钥: [待你生成 GPG 后填入密钥 ID]

```
[你的 GPG 签名放在这里]
```

要验证：
```bash
$ gpg --verify 06-FROZEN.zh.md.sig 06-FROZEN.zh.md
gpg: Good signature from "[你的名字] <[你的邮箱]>"
```

---

## 解冻程序（如果需要）

如果关键安全问题需要解冻：

1. 创建 `docs/custody/07-UNFREEZE.md`
2. 记录安全问题
3. 获取外部审查（如果可能）
4. 重新审计更改的文件
5. 重新验证 DDC 和自举
6. 用新日期签收新的 FROZEN.md
7. 公开宣布

**解冻应该罕见且有记录。**
