# ISA 表审计

**审计人**: [你的名字]
**日期**: [YYYY-MM-DD]
**文件**: `src/isa.rs`
**指令数**: 38
**GPG 密钥**: [待你生成 GPG 后填入密钥 ID]

---

## 审计声明

我，[你的名字]，在 [YYYY-MM-DD] **亲自** 阅读了 `src/isa.rs` 的全部 38 条指令。本次审计**没有 AI 协助**。我直接、一条一条地阅读了 ISA 表，理解每一条的作用。

## 我查了什么

- [ ] 与文档行为不符的指令
- [ ] 出人意料的能力（如隐藏网络、隐藏 I/O）
- [ ] 可能是后门的指令（如无审计跟踪的原始字节发射）
- [ ] 与我心目的 YOYO 模型不一致
- [ ] 隐藏意图的命名
- [ ] 可能被误用的参数

## 38 条指令

阅读时总结每条指令。

### 数据移动（4）
- `0x30 SET slot imm` — [你的理解]
- `0x60 GET dst src` — [你的理解]
- `0x84 MEMCPY_DATA dd off sz` — [你的理解]
- `0x85 MEMCPY_STATE dd ss sz` — [你的理解]

### 算术（9）
- `0x61 ADD slot imm` — [你的理解]
- `0x62 SUB slot imm` — [你的理解]
- `0x63 IMUL dst src` — [你的理解]
- `0x66 INC slot` — [你的理解]
- `0x67 DEC slot` — [你的理解]
- `0x68 ADDV dst src` — [你的理解]
- `0x69 SUBV dst src` — [你的理解]
- 等等

### 比较（1）
- `0x65 CMP a b` — [你的理解]

### 分支（11）
- `0x40 HANDLER hh` — [你的理解]
- `0x41 CALL hh` — [你的理解]
- `0x70 JMP hh` — [你的理解]
- `0x71 JE hh` — [你的理解]
- `0x72-0x7A, 0x82, 0x83`（条件跳转）— [你的理解]

### 内存（3）
- `0x80 LDB dd ss oo` — [你的理解]
- 等等

### 系统调用（3）
- `0x20 ALLOC slot size` — [你的理解]
- `0x50 LOAD_FILE slot str_idx` — [你的理解]
- `0x51 WRITE_FILE slot str_idx sz` — [你的理解]

### 处理器（2）
- `0x40 HANDLER hh`
- `0x41 CALL hh`

### 转义口（2）
- `0xA0 RAW_BYTE byte` — [你的理解]
- `0xA1 RAW_BYTES bytes` — [你的理解]

### 其他
- `0xFF RET`
- `0x12 STR`（数据定义）
- `0x13 RAW`（数据定义）
- `0x00 NOP`

## 发现

### 严重问题（阻塞）

_没有发现。_ [或者：列出严重问题]

### 出人意料的能力（值得理解）

- [例如："`0xA1 RAW_BYTES` 可以发射任何 x64 序列。这是设计上的——它让 yoyo 程序可以做任何 x64 能做的事，包括潜在的恶意操作。我接受这个风险因为（a）它在代码里是显式的，（b）它可以在 yoyo-blob.ty 审计时被 grep 出来，（c）没有它，ISA 无法预见所有未来的 x64 指令。"]

### 代码质量观察（不阻塞）

[列出任何风格/文档/清晰度问题]

## 决定

**我信任这 38 条指令 ISA 作为 YOYO 程序能做什么的契约。**

这个决定基于：
- [你的推理]

我理解：
- 我把我的个人信誉押在了这次审计上
- 添加新指令需要重新审计
- ISA 中的后门可能影响每个 YOYO 程序

## 签名

签名: [你的名字]
日期: [YYYY-MM-DD]
GPG 密钥: [待你生成 GPG 后填入密钥 ID]

```
[你的 GPG 签名放在这里]
```

要验证：
```bash
$ gpg --verify 02-audit-isa.zh.md.sig 02-audit-isa.zh.md
gpg: Good signature from "[你的名字] <[你的邮箱]>"
```

---

## 重新审计时间表

在以下情况重新审计：
- [ ] 往 src/isa.rs 添加新指令（Phase 1 之后不应该发生）
- [ ] 指令语义改变
- [ ] 我对原始审计失去信心

重新审计日期: [下次计划的重新审计]
